Project: Library

objective: 
1.books are stored in an array
2.all book objects have a unique id

instruction:
1.write a function that loops through the library array and displays each book on the page. manually add some books to array to see the display (table or their own 'card')
2.add a "New Book" button allowing users to input details for a new book to add to library. (author, title, number of pages, whether it's been read or anything else)
3.add a button on each book's display to remove book from the library.
4.add a button on each book's display to change its read status


